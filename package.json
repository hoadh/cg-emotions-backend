{
  "name": "cg-emotions-backend-ts",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy": "cp -rf ./src/app/views ./build/views",
    "build": "rm -rf ./build/* && npm run copy && tsc",
    "prod": "npm run build && node ./build/app.js",
    "live": "tsc -w & nodemon ./build/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.2",
    "express": "^4.17.1",
    "typescript": "^3.7.4"
  }
}
